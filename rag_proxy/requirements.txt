# FastAPI framework for building the web API
fastapi>=0.104.1
# Uvicorn server to run the FastAPI application
uvicorn>=0.24.0
# LangChain core for RAG orchestration
langchain>=0.1.0
langchain-core
langchain-community
# LangChain integration for HuggingFace models and embeddings
langchain-huggingface
# ChromaDB client to communicate with our vector database
chromadb>=0.4.18
# Sentence Transformers for converting text into vector embeddings
sentence-transformers>=2.2.2
# Pydantic for data validation and settings management
pydantic>=2.5.2
# Python-dotenv for reading environment variables from .env files
python-dotenv>=1.0.0
# PyTorch: The core library for running deep learning models locally
torch
# Transformers: Library to load and run LLMs like Phi-3
transformers>=4.41.0
# Accelerate: Optimizes model loading and inference speed
accelerate
# Bitsandbytes: Enables 4-bit quantization to shrink the model size
bitsandbytes
